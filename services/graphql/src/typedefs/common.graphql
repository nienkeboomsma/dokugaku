scalar JSON

type Query

type Mutation

enum ReadStatus {
  abandoned
  new
  read
  reading
  want_to_read
}

enum WorkType {
  manga
  novel
}

type UpdateReadStatusResponse {
  code: Int!
  success: Boolean!
  message: String!
  status: ReadStatus
}
