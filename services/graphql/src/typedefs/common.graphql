scalar JSON

type Query

type Mutation

type SetReadStatusResponse {
  code: Int!
  success: Boolean!
  message: String
  readStatus: ReadStatus
}

enum ReadStatus {
  ABANDONED
  NONE
  READ
  READING
  WANT_TO_READ
}

enum WorkType {
  MANGA
  NOVEL
}
